import { digestVerificationToken } from "../../../lib/promotionalVerification";
import { getSupabaseAdminClient } from "../../../lib/supabaseAdmin";
import styles from "../../post-free/postFree.module.css";
export const metadata={title:"Verify Your Free Job Post",robots:{index:false,follow:false}};
type Outcome="created"|"already_used"|"expired"|"invalid"|"unavailable";
export default async function Page({params}:{params:Promise<{token:string}>}){const token=(await params).token,db=getSupabaseAdminClient();let outcome:Outcome="invalid";if(db&&token&&token.length<=200){const result=await db.rpc("redeem_public_promotional_verification",{p_token_digest:digestVerificationToken(token)});outcome=(Array.isArray(result.data)?result.data[0]?.outcome:null)??"invalid";}const success=outcome==="created"||outcome==="already_used";return <main className={styles.verifyPage}><section className={success?styles.success:styles.verifyCard}><p className={styles.eyebrow}>Free First Job</p><h1>{success?"Your job has been submitted for review":"This verification link is unavailable"}</h1><p>{success?"Your email is verified. Our team will review your Free First Job before it goes live.":outcome==="expired"?"This link has expired. Your job was not submitted. Please return to the Free First Job page to check eligibility again.":"This link is invalid, revoked, or no longer available. No job was submitted."}</p></section></main>}
